node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json

# Runtime state — asla commit edilmemeli
scanner/data/
*.bak
*.pre-*.bak

# Lokal ajan hatiralari
.remember/

# Claude Code lokal ayarlari (kullaniciya ozgu)
.claude/

# macOS finder cope
.DS_Store

# API anahtarlari / secret — never commit (Codex 1ada068)
.env
.env.*
!.env.example
*.key
*.pem

# Install-time symlink created by the one-shot setup (Codex 1ada068).
# NOT: AGENTS.md halen tracked durumda — bu kural sadece kullanıcı dosyayi
# elle silerse repoda gozukmemesini saglar. Tracked'tan cikarmak istenirse
# `git rm --cached AGENTS.md` gerekir.
AGENTS.md

# Test coverage output (Konu 4) — npm run test:coverage:lcov uretir
scanner/coverage/
coverage/
*.lcov
